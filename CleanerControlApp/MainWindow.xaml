<Window x:Class="CleanerControlApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CleanerControlApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="1080" Width="1920"
        WindowStartupLocation="Manual" Left="0" Top="0"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Title Bar -->
            <RowDefinition Height="*"/>
            <!-- Content -->
            <RowDefinition Height="Auto"/>
            <!-- Status Bar -->
        </Grid.RowDefinitions>

        <!-- Top Title Bar with user box +5 boxes/buttons -->
        <Border Grid.Row="0" Background="#FF2D2D30" Padding="10" MinHeight="96">
            <DockPanel LastChildFill="False">
                <!-- User info box on the left -->
                <Border DockPanel.Dock="Left" Background="#FF3A3A3A" Padding="8" Margin="0,0,8,0" CornerRadius="4" Height="124">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <StackPanel Orientation="Vertical" Margin="0,0,8,0" VerticalAlignment="Center">
                            <TextBlock x:Name="CurrentUserNameText" Text="帳號: -" Foreground="White" FontWeight="Bold"/>
                            <TextBlock x:Name="CurrentUserRoleText" Text="權限: -" Foreground="White" FontSize="12"/>
                        </StackPanel>
                        <Button x:Name="LogoutButton" Content="登出" Margin="6,0,0,0" Padding="10,8" Height="80" Click="LogoutButton_Click"/>
                    </StackPanel>
                </Border>

                <!-- Right-side compact status box with three vertical indicators -->
                <!-- moved to be left-docked and placed before the nav buttons so it sits left of Home -->
                <Border DockPanel.Dock="Left" Background="#FF3A3A3A" Padding="8" Margin="0,0,8,0" CornerRadius="4" VerticalAlignment="Center">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <!-- Auto indicator -->
                        <Border x:Name="AutoIndicator" Width="120" Height="28" Background="#FFB0B0B0" CornerRadius="4" Margin="0,4">
                            <TextBlock Text="自動" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black" FontWeight="Bold"/>
                        </Border>
                        <!-- Warning indicator -->
                        <Border x:Name="WarningIndicator" Width="120" Height="28" Background="#FFB0B0B0" CornerRadius="4" Margin="0,4">
                            <TextBlock Text="警告" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black" FontWeight="Bold"/>
                        </Border>
                        <!-- Alarm indicator -->
                        <Border x:Name="AlarmIndicator" Width="120" Height="28" Background="#FFB0B0B0" CornerRadius="4" Margin="0,4">
                            <TextBlock Text="錯誤" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </Border>

                <UniformGrid Columns="8" Height="116" Width="1019">
                    <Button x:Name="BtnHome" Content="主畫面" Margin="0,10,0,10" Padding="12,10" Click="BtnHome_Click" Width="103" />
                    <Button x:Name="BtnManual" Content="手動操作" Margin="0,10,0,10" Padding="12,10" Click="BtnManual_Click" Width="105" />
                    <Button x:Name="BtnIO" Content="IO" Margin="0,10,0,10" Padding="12,10" Click="BtnIO_Click" Width="107" />
                    <Button x:Name="BtnAlarm" Content="Alarm" Margin="0,10,0,10" Padding="12,10" Click="BtnAlarm_Click" Width="107" />
                    <Button x:Name="BtnInfo" Content="資訊" Margin="0,10,0,10" Padding="12,10" Click="BtnInfo_Click" Width="109" />
                    <Button x:Name="BtnSetting" Content="設定" Margin="0,10,0,10" Padding="12,10" Click="BtnSetting_Click" Width="111" />
                    <Button x:Name="BtnUser" Content="使用者管理" Margin="0,10,0,10" Padding="12,10" Click="BtnUser_Click" Width="113" />
                    <Button x:Name="BtnDeveloper" Content="開發畫面" Margin="0,10,0,10" Padding="12,10" Click="BtnDeveloper_Click" Width="113" />
                </UniformGrid>

            </DockPanel>
        </Border>

        <!-- Main content area for switching modules -->
        <Border Grid.Row="1" Background="#FFF" Padding="8">
            <ContentControl x:Name="MainContent" />
        </Border>

        <!-- Status bar at bottom -->
        <StatusBar Grid.Row="2" Background="#FFEEEEEE">
            <StatusBarItem HorizontalAlignment="Left">
                <TextBlock x:Name="VersionTextBlock" Text="Version"/>
            </StatusBarItem>
            <!-- Additional status items can be added here -->
        </StatusBar>

    </Grid>
</Window>
