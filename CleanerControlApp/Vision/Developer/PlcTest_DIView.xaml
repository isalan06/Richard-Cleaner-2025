<UserControl x:Class="CleanerControlApp.Vision.Developer.PlcTest_DIView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
 mc:Ignorable="d">
 <Grid Background="White">
 <DockPanel Margin="8">
 <TextBlock Text="DI Status" DockPanel.Dock="Top" FontSize="18" FontWeight="SemiBold" Margin="4" />
 <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
 <ItemsControl ItemsSource="{Binding Groups}">
 <ItemsControl.ItemsPanel>
 <ItemsPanelTemplate>
 <UniformGrid Columns="3" />
 </ItemsPanelTemplate>
 </ItemsControl.ItemsPanel>

 <ItemsControl.ItemTemplate>
 <DataTemplate>
 <Border BorderBrush="#FFB0B0B0" BorderThickness="1" Margin="6" Padding="8" CornerRadius="6" Background="WhiteSmoke" Width="260">
 <StackPanel>
 <TextBlock Text="{Binding Header}" FontWeight="Bold" Margin="4" HorizontalAlignment="Center" />
 <Separator Margin="2" />
 <ItemsControl ItemsSource="{Binding Items}">
 <ItemsControl.ItemsPanel>
 <ItemsPanelTemplate>
 <StackPanel />
 </ItemsPanelTemplate>
 </ItemsControl.ItemsPanel>
 <ItemsControl.ItemTemplate>
 <DataTemplate>
 <DockPanel Margin="4" LastChildFill="True">
 <Ellipse Width="18" Height="18" Stroke="Gray" StrokeThickness="1" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0,0,6,0">
 <Ellipse.Style>
 <Style TargetType="Ellipse">
 <Setter Property="Fill" Value="Gray" />
 <Style.Triggers>
 <DataTrigger Binding="{Binding IsOn}" Value="True">
 <Setter Property="Fill" Value="LimeGreen" />
 </DataTrigger>
 </Style.Triggers>
 </Style>
 </Ellipse.Style>
 </Ellipse>
 <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
 <TextBlock Text="{Binding Address}" Width="44" />
 <TextBlock Text=" - " />
 <TextBlock Text="{Binding Description}" TextWrapping="Wrap" />
 </StackPanel>
 <!-- Toggle button removed per request; lamp control will be handled later -->
 </DockPanel>
 </DataTemplate>
 </ItemsControl.ItemTemplate>
 </ItemsControl>
 </StackPanel>
 </Border>
 </DataTemplate>
 </ItemsControl.ItemTemplate>
 </ItemsControl>
 </ScrollViewer>
 </DockPanel>
 </Grid>
</UserControl>