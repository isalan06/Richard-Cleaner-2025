<UserControl x:Class="CleanerControlApp.Vision.Developer.DevUnitParameterView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
 mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1200">
    <Grid Background="White" Margin="0,0,0,-51">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Main title above the boxes with action buttons next to the title -->
        <Grid Grid.Row="0" Margin="8,8,8,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="元件參數" FontSize="16" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Center" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Button x:Name="BtnLoadParams" Content="讀取參數" Padding="8,4" Margin="8,0,6,0" Click="BtnLoadParams_Click" />
                <Button x:Name="BtnWriteParams" Content="寫入參數" Padding="8,4" Margin="6,0,6,0" Click="BtnWriteParams_Click" />
                <Button x:Name="BtnSaveParams" Content="寫入參數檔" Padding="8,4" Click="BtnSaveParams_Click" />
            </StackPanel>
        </Grid>

        <!-- Two parameter boxes side by side: DryingTanks and Sink -->
        <Grid Grid.Row="1" Margin="8,4,8,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="300" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <!-- DryingTanks box (existing) -->
            <Border Grid.Column="0" Background="#FFF8F8F8" BorderBrush="#FFBDBDBD" BorderThickness="1" CornerRadius="6" Padding="8" HorizontalAlignment="Left">
                <StackPanel>
                    <TextBlock Text="烘乾槽參數" FontWeight="Bold" FontSize="14" Margin="0,0,0,6" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <!-- Select Tank -->
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                            <!-- UnitTransfer -->
                            <RowDefinition Height="Auto" />
                            <!-- SV Low -->
                            <RowDefinition Height="Auto" />
                            <!-- SV High -->
                            <RowDefinition Height="Auto" />
                            <!-- PV Low -->
                            <RowDefinition Height="Auto" />
                            <!-- PV High -->
                            <RowDefinition Height="Auto" />
                            <!-- Cover Open -->
                            <RowDefinition Height="Auto" />
                            <!-- Cover Close -->
                            <RowDefinition Height="Auto" />
                            <!-- SV Check -->
                            <RowDefinition Height="Auto" />
                            <!-- ActTime -->
                        </Grid.RowDefinitions>

                        <!-- Select Tank -->
                        <TextBlock Grid.Column="0" Text="Select Tank:" VerticalAlignment="Center" Margin="4,0,4,0" Height="15" />
                        <ComboBox Grid.Row="0" Grid.Column="1" x:Name="cmbDryingTankIndex" Width="120" HorizontalAlignment="Center" SelectionChanged="cmbDryingTankIndex_SelectionChanged" />

                        <!-- UnitTransfer -->
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="UnitTransfer:" VerticalAlignment="Center" Margin="4,0,4,0" Height="15" />
                        <TextBox Grid.Row="2" Grid.Column="1" x:Name="TxtUnitTransfer" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                        <!-- SV Low Limit -->
                        <TextBlock Grid.Row="3" Grid.Column="0" Text="SV Low Limit:" VerticalAlignment="Center" Margin="4,0,4,0" Height="16" />
                        <TextBox Grid.Row="3" Grid.Column="1" x:Name="TxtSVLowLimit" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                        <!-- SV High Limit -->
                        <TextBlock Grid.Row="4" Grid.Column="0" Text="SV High Limit:" VerticalAlignment="Center" Margin="4,0,4,0" Height="15" />
                        <TextBox Grid.Row="4" Grid.Column="1" x:Name="TxtSVHighLimit" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                        <!-- PV Low Timeout -->
                        <TextBlock Grid.Row="5" Grid.Column="0" Text="PV Low Timeout (s):" VerticalAlignment="Center" Margin="4,0,4,0" Height="15" />
                        <TextBox Grid.Row="5" Grid.Column="1" x:Name="TxtPVLowTimeout" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                        <!-- PV High Timeout -->
                        <TextBlock Grid.Row="6" Grid.Column="0" Text="PV High Timeout (s):" VerticalAlignment="Center" Margin="4,0,4,0" Height="15" />
                        <TextBox Grid.Row="6" Grid.Column="1" x:Name="TxtPVHighTimeout" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                        <!-- Cover Open Timeout -->
                        <TextBlock Grid.Row="7" Grid.Column="0" Text="Cover Open Timeout (s):" VerticalAlignment="Center" Margin="4,0,4,0" Height="16" />
                        <TextBox Grid.Row="7" Grid.Column="1" x:Name="TxtCoverOpenTimeout" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                        <!-- Cover Close Timeout -->
                        <TextBlock Grid.Row="8" Grid.Column="0" Text="Cover Close Timeout (s):" VerticalAlignment="Center" Margin="4,0,4,0" Height="15" />
                        <TextBox Grid.Row="8" Grid.Column="1" x:Name="TxtCoverCloseTimeout" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                        <!-- SV Check Offset -->
                        <TextBlock Grid.Row="9" Grid.Column="0" Text="SV Check Offset:" VerticalAlignment="Center" Margin="4,0,4,0" Height="15" />
                        <TextBox Grid.Row="9" Grid.Column="1" x:Name="TxtSVCheckOffset" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                        <!-- ActTime Limit -->
                        <TextBlock Grid.Row="10" Grid.Column="0" Text="ActTime Limit (s):" VerticalAlignment="Center" Margin="4,0,4,0" Height="15" />
                        <TextBox Grid.Row="10" Grid.Column="1" x:Name="TxtActTimeLimit" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                    </Grid>
                </StackPanel>
            </Border>

            <!-- Sink box (copied from drying tank, no Select Tank) -->
            <Border Grid.Column="2" Background="#FFF8F8F8" BorderBrush="#FFBDBDBD" BorderThickness="1" CornerRadius="6" Padding="8" HorizontalAlignment="Left">
                <StackPanel>
                    <TextBlock Text="沖水槽參數" FontWeight="Bold" FontSize="14" Margin="0,0,0,6" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <!-- Motor UnitTransfer -->
                            <RowDefinition Height="Auto" />
                            <!-- UnitTransfer -->
                            <RowDefinition Height="Auto" />
                            <!-- SV Low -->
                            <RowDefinition Height="Auto" />
                            <!-- SV High -->
                            <RowDefinition Height="Auto" />
                            <!-- PV Low -->
                            <RowDefinition Height="Auto" />
                            <!-- PV High -->
                            <RowDefinition Height="Auto" />
                            <!-- Cover Open -->
                            <RowDefinition Height="Auto" />
                            <!-- Cover Close -->
                            <RowDefinition Height="Auto" />
                            <!-- SV Check -->
                            <RowDefinition Height="Auto" />
                            <!-- ActTime -->
                        </Grid.RowDefinitions>

                        <!-- Motor UnitTransfer -->
                        <TextBlock Grid.Column="0" Text="Motor UnitTransfer:" VerticalAlignment="Center" Margin="4,0,4,0" Height="15" />
                        <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtSinkMotorUnitTransfer" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                        <!-- UnitTransfer -->
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="UnitTransfer:" VerticalAlignment="Center" Margin="4,0,4,0" Height="15" />
                        <TextBox Grid.Row="1" Grid.Column="1" x:Name="TxtSinkUnitTransfer" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                        <!-- SV Low Limit -->
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="SV Low Limit:" VerticalAlignment="Center" Margin="4,0,4,0" Height="16" />
                        <TextBox Grid.Row="2" Grid.Column="1" x:Name="TxtSinkSVLowLimit" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                        <!-- SV High Limit -->
                        <TextBlock Grid.Row="3" Grid.Column="0" Text="SV High Limit:" VerticalAlignment="Center" Margin="4,0,4,0" Height="15" />
                        <TextBox Grid.Row="3" Grid.Column="1" x:Name="TxtSinkSVHighLimit" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                        <!-- PV Low Timeout -->
                        <TextBlock Grid.Row="4" Grid.Column="0" Text="PV Low Timeout (s):" VerticalAlignment="Center" Margin="4,0,4,0" Height="15" />
                        <TextBox Grid.Row="4" Grid.Column="1" x:Name="TxtSinkPVLowTimeout" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                        <!-- PV High Timeout -->
                        <TextBlock Grid.Row="5" Grid.Column="0" Text="PV High Timeout (s):" VerticalAlignment="Center" Margin="4,0,4,0" Height="15" />
                        <TextBox Grid.Row="5" Grid.Column="1" x:Name="TxtSinkPVHighTimeout" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                        <!-- Cover Open Timeout -->
                        <TextBlock Grid.Row="6" Grid.Column="0" Text="Cover Open Timeout (s):" VerticalAlignment="Center" Margin="4,0,4,0" Height="16" />
                        <TextBox Grid.Row="6" Grid.Column="1" x:Name="TxtSinkCoverOpenTimeout" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                        <!-- Cover Close Timeout -->
                        <TextBlock Grid.Row="7" Grid.Column="0" Text="Cover Close Timeout (s):" VerticalAlignment="Center" Margin="4,0,4,0" Height="15" />
                        <TextBox Grid.Row="7" Grid.Column="1" x:Name="TxtSinkCoverCloseTimeout" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                        <!-- SV Check Offset -->
                        <TextBlock Grid.Row="8" Grid.Column="0" Text="SV Check Offset:" VerticalAlignment="Center" Margin="4,0,4,0" Height="15" />
                        <TextBox Grid.Row="8" Grid.Column="1" x:Name="TxtSinkSVCheckOffset" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                        <!-- ActTime Limit -->
                        <TextBlock Grid.Row="9" Grid.Column="0" Text="ActTime Limit (s):" VerticalAlignment="Center" Margin="4,0,4,0" Height="15" />
                        <TextBox Grid.Row="9" Grid.Column="1" x:Name="TxtSinkActTimeLimit" Width="120" HorizontalAlignment="Center" Margin="0,4,0,4" />

                    </Grid>
                </StackPanel>
            </Border>

        </Grid>

        <!-- Placeholder for rest of content -->
        <Grid Grid.Row="2" Margin="8">
            <!-- Empty for now; additional UI can be added later -->
        </Grid>
    </Grid>
</UserControl>