<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="CleanerControlApp.Vision.Developer.DevModuleTestView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
 mc:Ignorable="d"
 d:DesignHeight="600" d:DesignWidth="800">
  <Grid Background="White">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="180" />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>

    <!-- Left panel: module buttons -->
    <Border Grid.Column="0" Margin="8" Padding="8" BorderBrush="Gray" BorderThickness="1" CornerRadius="4" Background="#FFF">
      <StackPanel VerticalAlignment="Top" Margin="0,0,0,8">
        <Button x:Name="BtnSystem" Content="系統" Height="72" HorizontalAlignment="Stretch" Margin="0,0,0,6" Click="BtnSystem_Click" />
        <Button x:Name="BtnShuttle" Content="移載組" Height="72" HorizontalAlignment="Stretch" Margin="0,0,0,6" Click="BtnShuttle_Click" />
        <Button x:Name="BtnSink" Content="沖水槽" Height="72" HorizontalAlignment="Stretch" Margin="0,0,0,6" Click="BtnSink_Click" />
        <Button x:Name="BtnSoakingTank" Content="浸泡槽" Height="72" HorizontalAlignment="Stretch" Margin="0,0,0,6" Click="BtnSoakingTank_Click" />
        <Button x:Name="BtnDryingTank" Content="烘乾槽" Height="72" HorizontalAlignment="Stretch" Margin="0,0,0,6" Click="BtnDryingTank_Click" />
        <Button x:Name="BtnHeatingTank" Content="加熱槽" Height="72" HorizontalAlignment="Stretch" Margin="0,0,0,6" Click="BtnHeatingTank_Click" />
      </StackPanel>
    </Border>

    <!-- Right content area: ContentControl placeholder -->
    <Border Grid.Column="1" Margin="8" Padding="0" BorderBrush="#BDBDBD" BorderThickness="1" CornerRadius="4" Background="#FAFAFA" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
      <ContentControl x:Name="ModuleContentPlaceholder" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Margin="0" />
    </Border>
  </Grid>
</UserControl>